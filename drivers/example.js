// ref: URL or something else to identify where to pull from
// params: params for pulling, such as API key
// callback: callback to call once puling is done, or if there's an error.
//           Uses the signature: (err, data) where data is in the form of { activities: [Activity], ... }
// Database operations will be performed afterwards
exports.pull = function(ref, params, callback) {
  
};