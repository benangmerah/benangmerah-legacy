#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jsonld/bin/jsonld" "$@"
  ret=$?
else 
  node  "$basedir/../jsonld/bin/jsonld" "$@"
  ret=$?
fi
exit $ret
